# Action Log — 2026-05-31
_Auto-generated by Claude Code. Do not edit manually during an active session._

## [11:11] Create Session Documentation Directories

**Type**: create
**Files affected**:
- `docs/logs/` — ensured action log directory exists
- `docs/features/` — ensured feature documentation directory exists
**Command run** _(if applicable)_: `ls "/workspace" && mkdir -p "/workspace/docs/logs" "/workspace/docs/features"`
**Reason**: Prepare required audit and feature documentation locations before applying fixes.
**Rollback**: Delete `docs/logs/` and `docs/features/` if they contain no project documentation that should be retained.

## [11:22] Fix Critical Persistence and Profile Regressions

**Type**: modify
**Files affected**:
- `client/src/app/layout/App.tsx` — posts new activities to the API before local state updates and loads profiles by active username
- `client/src/feature/activities/form/ActivityForm.tsx` — awaits create persistence and preserves form values on failure
- `client/src/feature/activities/dashboard/ActivityDashboard.tsx` — propagates async create contract
- `Application/Profiles/Queries/GetUserProfile.cs` — returns requested profile identity instead of hardcoded Jeff display name
- `API/Program.cs` — rethrows migration/seeding startup failures in every environment
- `Persistence/DbInitializer.cs` — treats any existing table data as a seeded database guard
- `Tests/Application/Profiles/Queries/GetUserProfileHandlerTests.cs` — covers requested profile identity
- `Tests/Persistence/DbInitializerTests.cs` — covers partial seed guard behavior
- `client/src/app/layout/__tests__/App.test.tsx` — covers persisted username profile URL and persisted activity creation
- `client/src/feature/activities/form/__tests__/ActivityForm.test.tsx` — covers form preservation when create fails
- `client/src/feature/activities/dashboard/__tests__/ActivityDashboard.test.tsx` — updates async create test contract
**Reason**: Confirmed critical bugs caused data loss on activity creation, wrong-profile loading, hidden production startup failures, and repeated seeding after partial data.
**Rollback**: Revert these files with `git checkout -- <path>` before commit, or revert the final commit after it is created.

## [11:24] Document Fixed Persistence Features

**Type**: create
**Files affected**:
- `docs/features/activity-creation-profile-persistence.md` — documented API-backed create flow and profile identity behavior
- `docs/features/database-startup-safety.md` — documented startup fail-fast and seed guard behavior
**Reason**: Required developer-facing feature documentation for business logic changes.
**Rollback**: Delete `docs/features/activity-creation-profile-persistence.md` and `docs/features/database-startup-safety.md`.

## [11:28] Run Initial Validation

**Type**: run
**Files affected**:
- `client/node_modules/` — installed frontend dependencies for local validation
**Command run** _(if applicable)_: `dotnet test`; `npm run lint && npm run test -- --runInBand=false && npm run build`; `npm install`; `npm run lint && npm run test && npm run build`
**Reason**: Validate backend and frontend changes after first fix commit.
**Rollback**: Delete `client/node_modules/`; no source rollback required for validation commands.

## [11:31] Tighten Frontend Tests After Async Create

**Type**: modify
**Files affected**:
- `client/src/app/layout/__tests__/App.test.tsx` — uses direct form field changes for speed and selects the navbar profile menu by accessible role
**Reason**: Frontend tests exposed timeout and ambiguous text selection issues after the create handler became async.
**Rollback**: Revert `client/src/app/layout/__tests__/App.test.tsx` to the previous commit.

## [11:34] Run Frontend Validation and Backend Compile Check

**Type**: run
**Files affected**:
- `/tmp/dotnet` — installed temporary .NET SDK outside the repository for backend validation
**Command run** _(if applicable)_: `npm run lint && npm run test && npm run build`; `curl -fsSL https://dot.net/v1/dotnet-install.sh -o /tmp/dotnet-install.sh && bash /tmp/dotnet-install.sh --channel 10.0 --install-dir /tmp/dotnet`; `PATH="/tmp/dotnet:$PATH" dotnet test`
**Reason**: Confirm frontend checks pass and run backend tests in an environment that initially lacked `dotnet`.
**Rollback**: Delete `/tmp/dotnet` and `/tmp/dotnet-install.sh`; no source rollback required for validation commands.

## [11:36] Fix DbInitializer Test Namespace Collision

**Type**: modify
**Files affected**:
- `Tests/Persistence/DbInitializerTests.cs` — renamed test namespace so it does not shadow the application `Persistence` namespace
**Reason**: `dotnet test` showed that `Tests.Persistence` caused existing tests that reference `Persistence.AppDbContext` to resolve the wrong namespace.
**Rollback**: Revert `Tests/Persistence/DbInitializerTests.cs` to the previous commit.

## [11:39] Run Final Backend Validation

**Type**: run
**Files affected**:
- No repository files changed
**Command run** _(if applicable)_: `PATH="/tmp/dotnet:$PATH" dotnet test`
**Reason**: Verify backend tests pass after fixing the persistence test namespace collision.
**Rollback**: No source rollback required.

## Session Summary

**Type**: modify
**Files affected**:
- `client/src/app/layout/App.tsx` — fixed API-backed activity creation and active-user profile loading
- `client/src/feature/activities/form/ActivityForm.tsx` — preserved failed create submissions
- `client/src/feature/activities/dashboard/ActivityDashboard.tsx` — propagated async create contract
- `Application/Profiles/Queries/GetUserProfile.cs` — removed hardcoded profile display name
- `API/Program.cs` — made startup fail fast on migration/seed failures
- `Persistence/DbInitializer.cs` — fixed partial seed guard
- `Tests/...` — added or updated regression coverage
- `docs/features/...` — added developer-facing feature documentation
**Command run** _(if applicable)_: `npm run lint && npm run test && npm run build`; `PATH="/tmp/dotnet:$PATH" dotnet test`
**Reason**: Completed critical bug investigation and fixes with validation.
**Rollback**: Revert commits `049fdf4`, `417005f`, `05bd376`, and the final log commit if needed.
